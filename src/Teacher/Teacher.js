import React from "react";

const Teacher = (props) =>
{
        return (
            <div>
                <h1> the teacher name is: {props.title} samadi</h1>
            </div>
        );
};

export default Teacher;
